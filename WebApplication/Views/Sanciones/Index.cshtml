@model IEnumerable<CommonComponents.DTOs.SancionesDTO>

@{
    ViewBag.Title = "Index";
}
@section style
{
    @Styles.Render("~/bundles/datatablecss")
}
@section scripts
{
    @Scripts.Render("~/bundles/datatablejs")
}

<h2>Sanciones</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<div class="table-responsive">
    <table id="zero_config" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.san_dias_sancion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.san_fecha_inicio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.san_fecha_fin)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Prestamos.pre_usuario)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Prestamos.Usuarios.usu_nombre)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.san_dias_sancion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.san_fecha_inicio)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.san_fecha_fin)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Prestamos.pre_usuario)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Prestamos.Usuarios.usu_nombre)
                    </td>
                    <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.san_codigo }) |*@
                        @Html.ActionLink("Detalles", "Details", new { id = item.san_codigo })
                        @*@Html.ActionLink("Delete", "Delete", new { id = item.san_codigo })*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
